import styled from "styled-components";

export const Container = styled.div`
	height: 35rem;

	display: flex;
	align-items: center;
	justify-content: center;

	svg {
		height: 7rem;
		width: 7rem;

		circle {
			fill: var(--purple);
		}
	}
`